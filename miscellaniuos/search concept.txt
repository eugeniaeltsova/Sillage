User input
    │
    ▼
Extract: description + referenced perfume name + keywords for filtered search (notes, accords, perfumer's name, brand, year)
    │
    ├─► Embed description text → vector A
    └─► Look up referenced perfume → get notes_combined → embed → vector B
                │
                ▼
        Fuse vectors (average or weighted: 60% description, 40% reference)
                │
                ▼
        ANN search → top 200 (100?) candidates
                │
                ▼
        Soft filters (recency boost, gender match if specified)
                │
                ▼
        Re-rank by rating score (+re-ranking based on sales/aktuelles angebot, + adding a dark horse etc.)
                │
                ▼
        Return top 10 → if shop inventory exists, flag which are in stock

#updated search logic
User input
    │
    ▼
LLM extracts:
    - description text
    - referenced perfume names (0, 1 or many)
    - filter keywords (gender, year, notes, brand, perfumer)
    │
    ▼
Embed description → vector A
    │
    ▼
For each referenced perfume:
    └─► Look up in Qdrant by name → get notes_combined → embed → vector Bn
    │
    ▼
For each (vector A + vector Bn) pair → search Qdrant → top 50 candidates
If no reference perfume → vector A alone → top 200
    │
    ▼
Merge all result sets + deduplicate
    │
    ▼
Re-rank by frequency of appearance across result sets
    │
    ▼
Apply soft filters (gender, year range)
    │
    ▼
Re-rank by rating score
    │
    ▼
Inject dark horse (high ANN score, low rating count, niche)
    │
    ▼
Return top 10 + flag shop inventory if available